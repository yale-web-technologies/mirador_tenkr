<%= form_for(manifests_service) do |f| %>
  <% if manifests_service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manifests_service.errors.count, "error") %> prohibited this manifests_service from being saved:</h2>

      <ul>
      <% manifests_service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
