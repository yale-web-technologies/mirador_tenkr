<p id="notice"><%= notice %></p>

<h1>Manifests Canvases</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @manifests_canvases.each do |manifests_canvas| %>
      <tr>
        <td><%= link_to 'Show', manifests_canvas %></td>
        <td><%= link_to 'Edit', edit_manifests_canvas_path(manifests_canvas) %></td>
        <td><%= link_to 'Destroy', manifests_canvas, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Manifests Canvas', new_manifests_canvas_path %>
