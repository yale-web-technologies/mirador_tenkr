<%= form_for(manifests_canva) do |f| %>
  <% if manifests_canva.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manifests_canva.errors.count, "error") %> prohibited this manifests_canva from being saved:</h2>

      <ul>
      <% manifests_canva.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
