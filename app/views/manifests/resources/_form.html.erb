<%= form_for(manifests_resource) do |f| %>
  <% if manifests_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manifests_resource.errors.count, "error") %> prohibited this manifests_resource from being saved:</h2>

      <ul>
      <% manifests_resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
