<%= form_for(manifests_sequence) do |f| %>
  <% if manifests_sequence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(manifests_sequence.errors.count, "error") %> prohibited this manifests_sequence from being saved:</h2>

      <ul>
      <% manifests_sequence.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
